/**
 * Created by gfox on 30/10/2014.
 */
public class AssignPrimitives {

    public static void main(String[] args) {
     /*   int i = 7;
        char c = i;  //cant assign if not a compile time constant
        byte b = 1;
        short s = 4;
        s = b * b; //operands of mathematical operators will always promoted to at least an int,
        // return values will also be at least an int
        // Compound assignment operators(+=,<<<,>>>) will do an explicit cast
*/
      /*  int[] scores1 = { 1, 2, 3, 4, 5, 6};
        int scores2[] = {1,2};
        System.arraycopy(scores1, 2, scores2, 0, 2);
        for(int k :  scores2) System.out.print(k);*/
    }
}
